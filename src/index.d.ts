

  test('throws or handles invalid date input', () => {
    expect(() => timeAgo('invalid-date')).toThrow();
  });
});










































