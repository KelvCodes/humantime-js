
    }
  }

  return locale.startsWith('en') ? 'just now' : rtf.format(0, 'second');
}









































































